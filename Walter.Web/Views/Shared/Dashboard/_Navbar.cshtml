@using Microsoft.AspNetCore.Identity;
@using Walter.Core.Entities.User;

@inject UserManager<AppUser> UserManager

@{
    var user = await UserManager.GetUserAsync(User);
    var email = await UserManager.GetEmailAsync(user);
}


<!-- Navbar -->
<nav class="main-header navbar navbar-expand navbar-white navbar-light">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a asp-action="Index" class="nav-link">Home</a>
        </li>
    </ul>
    <ul class="navbar-nav ml-auto">
        <li class="nav-item mr-2">
            <a asp-action="Profile" asp-controller="Dashboard" asp-route-id="@user.Id" class="btn btn-block btn-info btn-sm">Profile</a>
        </li>
    </ul>
    <ul class="navbar-nav">
       
        <li class="nav-item">
            <form method="post" asp-action="Logout" >
                <button type="submit" class="btn btn-block btn-info btn-sm">Logout</button>
            </form>
            
        </li>
    </ul>
</nav>
<!-- /.navbar -->
